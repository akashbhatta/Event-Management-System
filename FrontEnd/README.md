# EventPro Nepal - Frontend (Flask)

EventPro Nepal is a Flask-based event management app for discovering and hosting events.

## Quick Start
```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python app.py
```
